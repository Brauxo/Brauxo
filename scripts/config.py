# Centralized configuration for AI Models

# Primary model (automatically updated and validated)
MODEL = "gemini-3.8-flash"

# Fallback model in case the primary model is temporarily overloaded (e.g. 503 error)
FALLBACK_MODEL = "gemini-3.8-flash"
